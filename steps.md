أهلاً بك يا دكتور. بناءً على كلامك الأخير، الخطة الآن أصبحت أكثر ذكاءً وأقل تكلفة على الـ Server بفضل فكرة الـ Google Drive، وسننفذها بنظام التوازي (Parallel) عشان مفيش أي حاجة تبوظ في الموقع القديم لحد ما الجديد يثبت كفاءته.

إليك الخطة الهندسية الكاملة والتفصيلية لكل مرحلة، خطوة بخطوة، وبدون أي تعقيد:

### 🛠️ المرحلة الأولى: حماية الموقع العام (Backward Compatibility)

**الهدف:** تجهيز قاعدة البيانات وتعديل الموقع ليُفرق بين الملفات "المعتمدة" والملفات "المعلقة"، عشان لما الصُناع يرفعوا ملفات جديدة، متظهرش للطلاب فوراً.

- **ماذا سنفعل بالظبط؟**

1. سنكتب كود "مؤقت" (Script) تدوس عليه أنت كإدمن مرة واحدة، وظيفته يلف على كل الملفات (Decks) الموجودة حالياً في قاعدة بياناتك ويعطيها `status: "approved"`، عشان متختفيش من الموقع.
2. سنعدل كود الصفحة الرئيسية (`js/main.js` و `js/db.js`) المسئول عن جلب الداتا للطلاب. هنضيف شرط إنه مستحيل يجلب أي ملف إلا لو كان `status == "approved"`.

- **النتيجة:** الموقع محمي تماماً. أي ملف جديد هينزل بحالة `pending` مستحيل أي طالب يشوفه.

### 🛡️ المرحلة الثانية: محرك المراجعة (Admin Moderation)

**الهدف:** تجهيز مكان في لوحة تحكم الإدمن عشان يوصلك فيه الملفات المرفوعة وتراجعها.

- **ماذا سنفعل بالظبط؟**

1. في صفحة `dashboard/manage_decks.html`، سنضيف قسم أو جدول جديد باسم "الملفات المعلقة (Pending)".
2. سنبرمج الكود في `js/manage_decks.js` ليجلب الملفات التي حالتها `pending`.
3. سنضيف زرين لكل ملف:

- **موافقة (Approve):** يحول حالة الملف إلى `approved` (فيظهر للطلاب فوراً) ويبعت إشعار التيليجرام العادي للطلاب.
- **رفض (Reject):** يحذف الملف من قاعدة البيانات تماماً.

### 🚀 المرحلة الثالثة: نظام الرفع الجديد للمُنشئين (New Creator Flow)

**الهدف:** عمل صفحة رفع جديدة كلياً للمنشئين تعمل بنظام الـ Google Drive، دون المساس بنظام المهام (Tasks) القديم.

- **ماذا سنفعل بالظبط؟**

1. سننشئ صفحة جديدة مثلاً `creators/submit_deck.html` وملف جافاسكريبت جديد `js/submit_deck.js`.
2. واجهة الرفع لن تحتوي على زر رفع ملف (Upload File). بدلاً من ذلك، سيكون هناك:

- اسم الملف (Title).
- المادة (Subject).
- **حقل نصي (Text Input):** يضع فيه المنشئ رابط جوجل درايف.

3. سنكتب كود جافاسكريبت يكتشف تلقائياً الـ ID الخاص بجوجل درايف من الرابط، ويحوله إلى رابط تحميل مباشر (Direct Download Link).
4. عند الحفظ في قاعدة البيانات (`decks`)، سيتم وضع بانر موحد إجبارياً، وحالة الملف ستكون `pending`، ويتم ربط الملف بالـ `uid` الخاص بالمنشئ.

### 🔔 المرحلة الرابعة: إشعارات الإدمن الفورية (Admin Alerts)

**الهدف:** إخبارك فوراً متى قام أي مُنشئ برفع ملف لكي لا يتأخر الاعتماد.

- **ماذا سنفعل بالظبط؟**

1. في ملف `js/telegram_service.js`، سنصنع دالة جديدة `notifyAdminOnSubmission()`.
2. هذه الدالة تستهدف الـ Telegram ID الخاص بك أنت فقط.
3. سيتم استدعاء هذه الدالة أوتوماتيكياً في نهاية المرحلة الثالثة بمجرد أن يضغط المنشئ على "إرسال".
4. أيضاً سننشئ إشعار (In-app Notification) يظهر في علامة الجرس داخل لوحة تحكم الإدمن.

### 📂 المرحلة الخامسة: إدارة ملفات المُنشئ (My Uploads)

**الهدف:** إعطاء الصانع لوحة تحكم خاصة بملفاته التي رفعها لكي لا يضطرك للتعديل عليها نيابة عنه.

- **ماذا سنفعل بالظبط؟**

1. سننشئ صفحة للمُنشئ تعرض جدولاً بكل الملفات التي رفعها هو شخصياً فقط (باستخدام `creatorId`).
2. الجدول سيعرض حالة الملف (معلق / مقبول).
3. سنضيف له أزراراً لـ: حَذف الملف أو تحديث الرابط (تعديل رابط جوجل درايف إذا اكتشف فيه خطأ).

### 🗑️ المرحلة السادسة: التنظيف الشامل (Sunsetting Old System)

**الهدف:** إزالة الكود القديم لتخفيف الموقع بعد التأكد من نجاح النظام الجديد 100%.

- **ماذا سنفعل بالظبط؟**

1. سنحذف قائمة "My Tasks" من لوحة المُنشئ، ونحذف صفحات إسناد المهام من لوحة الإدمن.
2. سنحذف أي ملفات HTML أو JS لم يعد لها قيمة.

---

إذا كانت الخطة متوافقة مع تفكيرك ورؤيتك 100%، رد عليّ بـ **"ابدأ المرحلة الأولى"**، وسأرسل لك الأكواد الدقيقة لتحديث الـ `status` وحماية جلب البيانات لتطبيقها الآن.
